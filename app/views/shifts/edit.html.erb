<div class="max-w-2xl">
  <h1 class="text-2xl font-bold text-gray-900 mb-2">Edit Shift</h1>
  <p class="text-sm text-gray-500 mb-6">
    Program: <%= link_to @program.name, program_path(@program), class: "text-indigo-600 hover:underline" %>
  </p>
  <%= render "form", shift: @shift %>
</div>
