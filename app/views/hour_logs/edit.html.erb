<div class="max-w-xl">
  <nav class="text-sm text-gray-500 mb-4">
    <%= link_to "Hours", hour_logs_path, class: "hover:underline" %> / Edit
  </nav>
  <h1 class="text-2xl font-bold text-gray-900 mb-6">Edit Hour Log</h1>
  <%= render "form", hour_log: @hour_log, url: hour_log_path(@hour_log),
      programs: @programs, submit_label: "Save Changes" %>
</div>
